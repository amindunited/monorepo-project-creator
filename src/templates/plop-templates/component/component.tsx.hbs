import React from 'react';
import { {{pascalCase name}}Container } from './{{pascalCase name}}.css';

/**

type {{pascalCase name}}Props = React.HTMLAttributes<HTMLDivElement> & {
  title?: string; // @todo - Remove this example if unused
};

*/

export interface {{pascalCase name}}Props extends React.HTMLAttributes<HTMLDivElement> {
  title?: string; // @todo - Remove this example if unused
};

export const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = ({ children, className, ...props }) => {
  const classNames = `${ {{pascalCase name}}Container } ${className || ''} `.trim();
  return (
    <div className={classNames} {...props}>
      {{pascalCase name}} Works
      {children}
    </div>
  );
};
